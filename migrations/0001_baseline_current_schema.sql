-- TOTISH database migration baseline.
--
-- Production already contains the schema represented by the codebase at the
-- time versioned migrations were introduced. This migration intentionally
-- changes no application tables; it records the starting point so every
-- future schema change can be tracked as 0002, 0003, and so on.

SELECT 1;
